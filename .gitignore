# Secrets - NEVER commit these
.env
credentials.json
client_secret*.json
token.json

# macOS
.DS_Store

# Node (if we add scripts later)
node_modules/

# Python (if we add scripts later)
__pycache__/
*.pyc
.venv/
venv/
